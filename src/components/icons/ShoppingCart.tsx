import { ShoppingCartOutlined } from "@mui/icons-material";
import type { IconProps } from "./types";

export default function ShoppingCarIcon(props: IconProps) {
    return <ShoppingCartOutlined {...props} />;
}